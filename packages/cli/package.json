{
  "name": "@ui-manifest-json/cli",
  "version": "0.2.0",
  "description": "One CLI entry point that dispatches to whichever @ui-manifest extractor(s) are installed.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20.6.0"
  },
  "bin": {
    "ui-manifest": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@ui-manifest-json/core": "^0.2.0"
  },
  "peerDependencies": {
    "@ui-manifest-json/angular": "^0.1.0",
    "@ui-manifest-json/react": "^0.1.0"
  },
  "peerDependenciesMeta": {
    "@ui-manifest-json/angular": {
      "optional": true
    },
    "@ui-manifest-json/react": {
      "optional": true
    }
  },
  "devDependencies": {
    "@ui-manifest-json/angular": "^0.1.0",
    "@ui-manifest-json/react": "^0.1.0",
    "typescript": "^5.5.0"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  }
}
